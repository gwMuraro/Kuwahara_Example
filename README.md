# kuwahara example 
Simple script copying the documentation of pykuwahara example. 

## install 
pip install -r requirements.txt

## use 

1. Put images in the folder `source_images`
2. launch the example script :
```py 
python kuwahara_example.py 
```
3. results should appear in `result_images/<name of your image>/`
